# دليل نشر DreamDrift على متجر جوجل بلاي (عبر TWA)

هذا الدليل يغطّي الخطوات التي **تتطلّب أدوات بناء أندرويد حقيقية** (Node.js محلي، JDK، Android SDK) لا يمكن تنفيذها من بيئة تطوير هذا المشروع نفسها. كل خطوة أخرى (سياسة الخصوصية، manifest.json، أيقونات PWA) جاهزة بالفعل في المستودع.

## لماذا TWA؟

**Trusted Web Activity** تقنية مدعومة رسميًا من جوجل تُغلِّف تطبيق الويب التقدّمي (PWA) **كما هو تمامًا** داخل حزمة أندرويد — بلا إعادة كتابة أي كود، وبلا كسر فلسفة "ملف واحد بلا خطوة بناء" التي بُني عليها DreamDrift. المتصفح (Chrome) يُشغِّل الموقع الحقيقي داخل التطبيق، بلا شريط عنوان ظاهر، فيبدو تطبيقًا أصليًا كاملًا.

---

## المتطلبات الأساسية (على جهازك المحلي، لا هنا)

| الأداة | الغرض | رابط التثبيت |
|---|---|---|
| Node.js (18+) | تشغيل Bubblewrap CLI | nodejs.org |
| JDK (11+) | بناء وتوقيع حزمة أندرويد | adoptium.net |
| Android SDK | (يُثبَّت تلقائيًا عبر Bubblewrap أول مرة) | — |

```bash
npm install -g @bubblewrap/cli
```

---

## الخطوة ١ — تأكد أن الموقع منشور فعليًا

قبل البدء، تحقّق أن هذه الروابط تعمل فعليًا (بعد رفع آخر نسخة من هذا المستودع):

- `https://fmobarki.github.io/DreamDrift/manifest.json`
- `https://fmobarki.github.io/DreamDrift/legal/privacy.html`

## الخطوة ٢ — تهيئة مشروع Bubblewrap

```bash
mkdir dreamdrift-android && cd dreamdrift-android
bubblewrap init --manifest="https://fmobarki.github.io/DreamDrift/manifest.json"
```

سيسألك Bubblewrap عن:
- **Package name**: اقترح `app.dreamdrift.twa` (يُستخدَم أيضًا في `assetlinks.json` الموجود بالفعل في المستودع — إن غيّرته، حدِّث الملف بالقيمة نفسها).
- **App name / Short name**: تُقرأ تلقائيًا من manifest.json.
- **Signing key**: اختر "Create a new key" إن كانت أول مرة، واحفظ كلمة المرور في مكان آمن جدًا — فقدانها يعني عدم القدرة على نشر تحديثات لنفس التطبيق مستقبلًا أبدًا.

## الخطوة ٣ — استخراج بصمة التوقيع (SHA-256)

```bash
keytool -list -v -keystore ./android.keystore -alias android
```

انسخ قيمة **SHA256** الظاهرة (تُشبه `14:6D:E9:83:C5:73:06:50:D8:EE:B9:95:2F:34:FC:64:16:A9:B4:...`).

## الخطوة ٤ — تحديث assetlinks.json بالبصمة الحقيقية

افتح `.well-known/assetlinks.json` في هذا المستودع، واستبدل:
```
"REPLACE_WITH_YOUR_SHA256_FINGERPRINT_AFTER_BUILDING_WITH_BUBBLEWRAP"
```
بالبصمة الفعلية التي نسختها (بصيغة `AA:BB:CC:...` كما هي، أو بلا نقطتين — كلاهما مقبول). **ارفع هذا التعديل** إلى GitHub Pages — لا يعمل التطبيق بلا شريط المتصفح إلا بعد أن يصبح هذا الملف منشورًا فعليًا وقابلًا للقراءة على:
```
https://fmobarki.github.io/DreamDrift/.well-known/assetlinks.json
```

## الخطوة ٥ — البناء النهائي

```bash
bubblewrap build
```

سيُنتج ملفين: `app-release-signed.apk` (للاختبار المباشر على جهازك) و`app-release-bundle.aab` (**هذا ما يُرفَع على Play Console**، لا الـ APK).

## الخطوة ٦ — اختبار محلي قبل الرفع

```bash
adb install app-release-signed.apk
```
افتح التطبيق على جهاز أندرويد حقيقي (أو محاكي) وتأكد:
- لا يظهر أي شريط عنوان متصفح (يعني أن `assetlinks.json` يعمل بشكل صحيح).
- كل الشاشات والإشعارات تعمل كما في نسخة الويب تمامًا.

---

## الخطوة ٧ — حساب Google Play Console

1. أنشئ حسابًا على [play.google.com/console](https://play.google.com/console) (رسم لمرة واحدة: ٢٥$).
2. أنشئ تطبيقًا جديدًا (App) → اختر "App" لا "Game".

## الخطوة ٨ — تعبئة قائمة المتجر (Store Listing)

| الحقل | القيمة المقترحة |
|---|---|
| اسم التطبيق | DreamDrift — سُكون |
| وصف قصير (٨٠ حرفًا) | رفيقك الليلي لتتبّع النوم والمزاج والتنفّس والاسترخاء |
| وصف كامل | استخدم فقرات "نقاط القوة" من التقرير الذي راجعناه سابقًا (تصميم Glassmorphism، تحليل نوم متقدّم، خصوصية كاملة، عمل دون إنترنت) |
| رابط سياسة الخصوصية | `https://fmobarki.github.io/DreamDrift/legal/privacy.html` |
| التصنيف | الصحة واللياقة (Health & Fitness) |
| أيقونة عالية الدقة (٥١٢×٥١٢) | **يحتاج تجهيزًا — راجع القسم أدناه** |
| الصورة الترويجية (Feature Graphic، ١٠٢٤×٥٠٠) | **يحتاج تجهيزًا — لا يوجد حاليًا** |
| لقطات شاشة (٢ على الأقل، بأبعاد هاتف) | **تحتاج تصويرًا فعليًا من جهاز حقيقي أو محاكي** — الأنسب: الشاشة الرئيسية، شاشة النوم، شاشة رحلتي/التحليل التفصيلي |

## الخطوة ٩ — استمارة أمان البيانات (Data Safety)

راجع ملف `DATA_SAFETY_ANSWERS.md` المرفق في هذا المستودع — يحتوي الإجابات الدقيقة جاهزة لنسخها مباشرة في الاستمارة.

## الخطوة ١٠ — تصنيف المحتوى (Content Rating)

أجب استبيان IARC داخل Play Console بصدق — التطبيق لا يحتوي عنفًا أو محتوى بالغين، يجب أن يحصل على تصنيف "للجميع" (Everyone/PEGI 3) بلا تعقيد.

## الخطوة ١١ — الرفع والإصدار

1. ارفع ملف `app-release-bundle.aab` (من الخطوة ٥) في قسم "Production" أو ابدأ بـ"Internal testing" أولًا (يُنصَح به دائمًا لأول رفعة).
2. انتظر مراجعة جوجل (عادة ساعات قليلة إلى يومين لتطبيق جديد).

---

## ملاحظة مهمة حول التحديثات المستقبلية

بما أن TWA تعرض الموقع مباشرة، **أي تحديث تنشره على GitHub Pages يظهر فورًا لكل مستخدمي تطبيق أندرويد بلا حاجة لرفع نسخة جديدة على المتجر** — إلا إذا غيّرت `manifest.json` نفسه أو احتجت رفع حزمة `.aab` جديدة لسبب آخر (نادر).
