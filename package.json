{
  "name": "dreamdrift",
  "version": "1.3.0",
  "private": true,
  "description": "رفيق ليلي لتتبّع النوم والمزاج — تطبيق ويب تقدّمي بملف واحد، بلا خطوة بناء.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "lint": "node scripts/extract-for-lint.mjs && eslint .lint-extracted.js; rm -f .lint-extracted.js"
  },
  "devDependencies": {
    "eslint": "^10.8.0"
  }
}
